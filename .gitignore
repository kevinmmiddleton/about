.DS_Store
*.swp
*.swo
*~
.idea/
.vscode/
node_modules/
.claude/

# editor/tooling backups; these were untracked cruft that a blanket
# `git add -A` swept into the relaunch branch
*.bak
*.orig

# OG export build output. The JPEGs are the reviewable artefact and stay
# tracked; the lossless PNGs are regenerable with `node tools/og-export.mjs`.
dist/og/*.png
